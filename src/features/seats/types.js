/**
 * @typedef {'available' | 'selected' | 'occupied' | 'vip'} SeatStatus
 */

/**
 * @typedef {Object} Seat
 * @property {string} id
 * @property {string} row
 * @property {number} number
 * @property {SeatStatus} status
 * @property {boolean} isVip
 * @property {number} price
 */

export {};
